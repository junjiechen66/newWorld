- 采用 Vite 构建，通过 `vite.config.js` 配置代理将 `/api` 请求转发至后端服务。
- 使用 Pinia 进行全局状态管理（如 `auth.js` 处理登录态），Vue Router 实现路由守卫与页面导航。
- 网络层基于 Axios 封装 `request.js`，统一处理 Bearer Token 注入及 HTTP 错误拦截。
- 界面层采用 Element Plus 组件库，遵循 `src/api`（接口定义）、`src/stores`（状态逻辑）、`src/views`（页面视图）的分层结构。