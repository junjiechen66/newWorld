- 所有控制器方法返回统一的Result<T>泛型对象，通过Result.success()或Result.error()静态方法构建响应。
- 业务异常通过抛出BusinessException处理，由GlobalExceptionHandler统一捕获并转换为标准错误响应。
- 实体类使用Lombok注解简化代码，并配合MyBatis-Plus的@TableId和@TableField注解映射数据库字段。
- 服务层采用接口（Service）与实现类（ServiceImpl）分离的模式，便于扩展与单元测试。